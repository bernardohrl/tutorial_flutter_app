import 'package:flutter/material.dart';

class CreditoGarantia extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
            title: Text('Crédito com Garantia Imobiliária'),
      ),
      body: Container(
        child: Center(
          child: Text('Entrou!!'),
        ),
      ),
    );
  }
}
